# Slim requirements for the LIVE server (PythonAnywhere).
# The deployed web app only READS forecasts/anomalies from the database, so the
# heavy ML libraries (prophet, scikit-learn) are NOT needed online — they only
# run on your laptop when you (re)train with train_models.py.

Flask
Flask-SQLAlchemy
Flask-Login
Flask-Migrate
Flask-WTF
email-validator
python-dotenv
pymysql
cryptography
pandas
openpyxl
reportlab
